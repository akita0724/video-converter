name: video_converter
description: Convert video files to different formats like mp3, mp4, mpg

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.19.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  file_picker: ^8.0.3
  process_run: ^0.14.2
  # ffmpeg_helper: ^1.0.1
  ffmpeg_cli: ^0.3.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
